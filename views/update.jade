extends layout

prepend content
        
    section
        article
            div.createbox
                form(name="update", action="/api/u", method="post")
                    input(type="hidden", name="id" value="#{story._id}")
                    input(type="text", name="title" placeholder="Title" value="#{story.title}")
                    textarea(name='content', rows=10, cols=50) #{story.content}
                    input(type="submit", value="update")
            
        
